import classes
import pixie

##
## Base class for image data
class BaseImage:

    ## Width in pixels
    var width = 0

    ## Height in pixels
    var height = 0

    ## Create from image data
    method fromData(data: string, )

    